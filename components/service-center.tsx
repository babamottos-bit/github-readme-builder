'use client'
import { useState } from 'react'
import { ArrowUpRight, CheckCircle2, CircleHelp, Flag, HeartHandshake, ShieldCheck, Users } from 'lucide-react'
import { serviceCatalog, subscriptionPlans } from '@/lib/service-center'

export function ServiceCenter() {
  const [filter, setFilter] = useState('all')
  const visible = filter === 'all' ? serviceCatalog : serviceCatalog.filter(item => item.category === filter)
  return <section id="service-center" className="border-y border-border bg-secondary/30"><div className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="max-w-2xl"><p className="mb-2 text-xs font-bold uppercase tracking-[0.16em] text-accent-foreground">32TechHub services</p><h2 className="text-4xl font-semibold tracking-tight">One place to work, connect, contribute, and get support.</h2><p className="mt-4 leading-7 text-muted-foreground">Fast pathways for employers, workers, communities, tenants, landlords, and public-service partners. Results are facilitated and referred; final agreements remain between the relevant parties.</p></div><div className="mt-8 flex flex-wrap gap-2">{['all','marketplace','community','support','rights'].map(item => <button key={item} onClick={() => setFilter(item)} className={`rounded-full border px-4 py-2 text-sm font-semibold capitalize ${filter === item ? 'border-primary bg-primary text-primary-foreground' : 'border-border bg-card'}`}>{item}</button>)}</div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{visible.map(item => <article key={item.id} className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between"><span className="grid size-10 place-items-center rounded-xl bg-secondary text-accent-foreground">{item.category === 'marketplace' ? <Users className="size-5" /> : item.category === 'support' ? <CircleHelp className="size-5" /> : item.category === 'rights' ? <ShieldCheck className="size-5" /> : <HeartHandshake className="size-5" />}</span><CheckCircle2 className="size-4 text-accent-foreground" /></div><h3 className="mt-6 text-lg font-semibold">{item.title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{item.description}</p><div className="mt-5 flex flex-wrap gap-2">{item.functions.map(fn => <span key={fn} className="rounded-md bg-secondary px-2 py-1 text-xs text-muted-foreground">{fn}</span>)}</div><button className="mt-6 inline-flex items-center gap-2 text-sm font-semibold">Open pathway <ArrowUpRight className="size-4" /></button></article>)}</div><div id="subscriptions" className="mt-16 rounded-3xl border border-border bg-card p-6"><div className="flex items-start justify-between gap-5"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-accent-foreground">Membership</p><h3 className="mt-2 text-2xl font-semibold">Choose your access level</h3><p className="mt-2 text-sm text-muted-foreground">Billing is coming soon. No charges are made in this preview.</p></div><Flag className="size-5 text-accent-foreground" /></div><div className="mt-6 grid gap-3 md:grid-cols-3">{subscriptionPlans.map(plan => <div key={plan.id} className="rounded-2xl border border-border p-4"><p className="font-semibold">{plan.name}</p><p className="mt-1 text-sm text-muted-foreground">{plan.price}</p><ul className="mt-4 space-y-2 text-sm text-muted-foreground">{plan.privileges.map(privilege => <li key={privilege}>• {privilege}</li>)}</ul><button className="mt-5 w-full rounded-xl border border-border px-3 py-2 text-sm font-semibold">Select plan</button></div>)}</div></div></div></section>
}
