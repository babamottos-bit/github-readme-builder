'use client'

import { useMemo, useState } from 'react'
import { ArrowUpRight, CheckCircle2, Clock3, Rss, ShieldCheck, Sparkles } from 'lucide-react'
import { releaseCategories, technologyReleases } from '@/lib/releases'

export function ReleaseHub() {
  const [category, setCategory] = useState<(typeof releaseCategories)[number]>('All updates')
  const [adminMode, setAdminMode] = useState(false)
  const releases = useMemo(() => category === 'All updates' ? technologyReleases : technologyReleases.filter(item => item.category === category), [category])
  return <section id="releases" className="border-y border-border bg-primary text-primary-foreground"><div className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="flex flex-col gap-8 lg:flex-row lg:items-end lg:justify-between"><div className="max-w-2xl"><div className="mb-3 flex items-center gap-2 text-xs font-bold uppercase tracking-[0.16em] text-accent"><Rss className="size-4" /> Release intelligence</div><h2 className="text-4xl font-semibold tracking-[-0.04em]">Stay current. Adapt with confidence.</h2><p className="mt-4 leading-7 text-primary-foreground/70">A curated release desk for the technologies shaping daily work, careers, and business — with practical guidance, source tracking, and an approval path for admins.</p></div><button onClick={() => setAdminMode(!adminMode)} className="inline-flex items-center gap-2 self-start rounded-xl border border-primary-foreground/20 px-4 py-2.5 text-sm font-semibold hover:bg-primary-foreground/10"><ShieldCheck className="size-4" /> {adminMode ? 'Exit admin review' : 'Admin review'}</button></div><div className="mt-9 flex gap-2 overflow-x-auto pb-2">{releaseCategories.map(item => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full px-3 py-2 text-xs font-semibold ${category === item ? 'bg-accent text-accent-foreground' : 'bg-primary-foreground/10 text-primary-foreground/70'}`}>{item}</button>)}</div><div className="mt-7 grid gap-4 lg:grid-cols-2">{releases.map(item => <article key={item.id} className="rounded-2xl border border-primary-foreground/15 bg-primary-foreground/[.06] p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold text-accent">{item.category}</p><h3 className="mt-2 text-lg font-semibold">{item.name}</h3><p className="mt-1 text-xs text-primary-foreground/50">{item.version} · {item.released}</p></div><span className="inline-flex items-center gap-1 rounded-full bg-primary-foreground/10 px-2.5 py-1 text-[11px] font-semibold"><CheckCircle2 className="size-3" /> {item.status}</span></div><div className="mt-5 grid gap-4 border-t border-primary-foreground/10 pt-4 sm:grid-cols-2"><div><p className="text-[11px] uppercase tracking-wide text-primary-foreground/45">What it does</p><p className="mt-1 text-sm leading-6 text-primary-foreground/80">{item.function}</p></div><div><p className="text-[11px] uppercase tracking-wide text-primary-foreground/45">Adapt it</p><p className="mt-1 text-sm leading-6 text-primary-foreground/80">{item.businessAdaptation}</p></div></div><div className="mt-5 flex items-center justify-between text-xs text-primary-foreground/50"><span className="inline-flex items-center gap-1"><Clock3 className="size-3" /> {item.dailyAdaptation}</span><button className="inline-flex items-center gap-1 font-semibold text-accent">View guidance <ArrowUpRight className="size-3" /></button></div>{adminMode && <div className="mt-4 rounded-lg border border-accent/30 bg-accent/10 px-3 py-2 text-xs text-primary-foreground/80"><Sparkles className="mr-1 inline size-3" /> Admin queue: approve source, edit adaptation, or schedule publication.</div>}</article>)}</div><div className="mt-6 flex items-center gap-2 text-xs text-primary-foreground/50"><Rss className="size-3.5" /> Feed ingestion + editorial approval architecture ready for live sources.</div></div></section>
}
